# Staging gate — human clearances for celebrated-proof flags.
#
# A mechanical conditional/incomplete flag on a celebrated AI-solved proof
# (CELEBRATED_PROBLEMS in erdos_frontier.py: AlphaProof's set + the Aristotle/GPT
# trio) is HELD out of the public feed until a human loads the actual proof and
# confirms the flag is real. List a problem number here once you have verified its
# flag by hand; only then does it publish as a discrepancy.
#
# Empty = every celebrated flag is held. As of 2026-06-30 none of the celebrated
# proofs flags at all (all audit unconditional), so nothing is currently held.
cleared: []
